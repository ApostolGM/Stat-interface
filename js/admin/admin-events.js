// admin/admin-events.js (заглушка)
import { log } from '../shared.js';

export function renderEventsAdmin() {
    const inner = document.getElementById('adminPanelContent');
    inner.innerHTML = '<h3>СОБЫТИЯ</h3><p>Модуль в разработке</p>';
}
