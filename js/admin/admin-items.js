// admin/admin-items.js (заглушка)
import { log } from '../shared.js';

export function renderItemsAdmin() {
    const inner = document.getElementById('adminPanelContent');
    inner.innerHTML = '<h3>БИБЛИОТЕКА ПРЕДМЕТОВ</h3><p>Модуль в разработке</p>';
}
